import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#99D3FB',
      light: '#99D3FB',
      dark: '#99D3FB',
    },
    secondary: {
      main: '#256BFE',
      light: '#256BFE',
      dark: '#256BFE',
    },
  },
});
export default theme;
